---
phase: 01-mobile-app-shell
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - playwright.config.ts
  - src/App.test.tsx
  - tests/mobile-shell.spec.ts
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
  - PRIV-01
  - PRIV-02
user_setup: []
must_haves:
  truths:
    - "Automated tests prove the home screen, fixed demo, parent info sheet, record empty state, and no-account flow."
    - "Mobile checks cover at least 320px and 375px wide viewports with no horizontal scroll."
    - "Privacy checks prove no account, analytics, child profile, microphone, camera, or storage writes exist in Phase 1 code."
  artifacts:
    - "vitest.config.ts exists."
    - "playwright.config.ts exists."
    - "src/App.test.tsx exists."
    - "tests/mobile-shell.spec.ts exists."
  key_links:
    - "package.json scripts `test` and `test:e2e` run Vitest and Playwright respectively."
    - "Playwright tests start or target the Vite app and exercise `开始玩`, `给家长`, and `看看记录`."
---

<objective>
Add automated validation for the Phase 1 mobile shell.

Purpose: Make the shell executable with confidence: build, unit interactions, mobile browser checks, and source assertions for privacy constraints.
Output: Vitest setup, Playwright setup, automated tests, and package scripts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-mobile-app-shell/01-CONTEXT.md
@.planning/phases/01-mobile-app-shell/01-UI-SPEC.md
@.planning/phases/01-mobile-app-shell/01-RESEARCH.md
@.planning/phases/01-mobile-app-shell/01-VALIDATION.md
@.planning/phases/01-mobile-app-shell/01-SKELETON.md
@.planning/phases/01-mobile-app-shell/01-01-SUMMARY.md
@.planning/phases/01-mobile-app-shell/01-02-SUMMARY.md
@AGENTS.md
</context>

<threat_model>
Trust boundaries:
- Test runner boundary: tests inspect source and browser behavior but do not call external services.
- Browser boundary: Playwright emulates mobile viewports and must not require real child data or accounts.

STRIDE register:
- T-01-05 Repudiation/verification gap: app appears compliant but privacy regressions are not checked. Mitigation: source assertions fail if banned account/storage/analytics/camera/microphone terms appear.
- T-01-06 UI spoofing/misleading incomplete features: record entry could imply real tracking. Mitigation: tests assert record entry shows `还没有记录` empty state and no storage writes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Configure Vitest component tests</name>
  <files>package.json, package-lock.json, vitest.config.ts, src/App.test.tsx</files>
  <read_first>package.json, src/App.tsx, .planning/phases/01-mobile-app-shell/01-VALIDATION.md, .planning/phases/01-mobile-app-shell/01-CONTEXT.md</read_first>
  <action>Add Vitest, jsdom, and Testing Library dependencies. Add package script `test` = `vitest`. Create `vitest.config.ts` with jsdom environment. Create `src/App.test.tsx` tests that render App and assert: home shows `小小认知`, `今天玩一点点`, `开始玩`, `看看记录`; clicking `开始玩` shows `找一找：苹果在哪里？`; clicking `看看记录` shows `还没有记录`; clicking parent info control shows `给家长`; source-level test or test helper asserts app source does not contain `localStorage`, `indexedDB`, `document.cookie`, `analytics`, `microphone`, `camera`, `login`, or `signup`.</action>
  <verify>npm run test -- --run</verify>
  <acceptance_criteria>
    - `package.json` contains `"test": "vitest"`.
    - `vitest.config.ts` contains `environment: 'jsdom'` or equivalent jsdom configuration.
    - `src/App.test.tsx` contains `小小认知`, `开始玩`, `找一找：苹果在哪里？`, `还没有记录`, and `给家长`.
    - `src/App.test.tsx` checks at least one banned privacy/storage term.
    - `npm run test -- --run` exits 0.
  </acceptance_criteria>
  <done>Unit/component tests prove core shell interactions and privacy source constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Playwright mobile shell tests</name>
  <files>package.json, package-lock.json, playwright.config.ts, tests/mobile-shell.spec.ts</files>
  <read_first>package.json, src/App.tsx, .planning/phases/01-mobile-app-shell/01-VALIDATION.md, .planning/phases/01-mobile-app-shell/01-UI-SPEC.md</read_first>
  <action>Add Playwright dependency and package script `test:e2e` = `playwright test`. Create `playwright.config.ts` with webServer command `npm run dev -- --host 127.0.0.1`, baseURL `http://127.0.0.1:5173`, and at least Chromium mobile-sized projects or tests for 320x640 and 375x667 viewports. Create `tests/mobile-shell.spec.ts` that opens `/`, asserts no horizontal scroll (`document.documentElement.scrollWidth <= window.innerWidth`), starts demo with `开始玩`, verifies `找一找：苹果在哪里？`, opens parent info and record empty state, and checks no visible login/signup/account prompt exists.</action>
  <verify>npm run test:e2e</verify>
  <acceptance_criteria>
    - `package.json` contains `"test:e2e": "playwright test"`.
    - `playwright.config.ts` contains `webServer`.
    - `playwright.config.ts` contains `5173`.
    - `tests/mobile-shell.spec.ts` contains `320` and `375` or equivalent viewport project references.
    - `tests/mobile-shell.spec.ts` contains `scrollWidth`.
    - `tests/mobile-shell.spec.ts` contains `开始玩`, `给家长`, and `看看记录`.
    - `npm run test:e2e` exits 0.
  </acceptance_criteria>
  <done>Browser tests verify the mobile shell flow and viewport constraints.</done>
</task>

<task type="auto">
  <name>Task 3: Run full validation and document commands</name>
  <files>package.json, README.md</files>
  <read_first>package.json, README.md, .planning/phases/01-mobile-app-shell/01-VALIDATION.md</read_first>
  <action>Add or update `README.md` with local commands: `npm install`, `npm run dev`, `npm run build`, `npm run test -- --run`, and `npm run test:e2e`. Run the full validation command `npm run build && npm run test -- --run && npm run test:e2e`. Do not add deployment hosting or CI in Phase 1.</action>
  <verify>npm run build && npm run test -- --run && npm run test:e2e</verify>
  <acceptance_criteria>
    - `README.md` contains `npm run dev`.
    - `README.md` contains `npm run build`.
    - `README.md` contains `npm run test -- --run`.
    - `README.md` contains `npm run test:e2e`.
    - `npm run build && npm run test -- --run && npm run test:e2e` exits 0.
  </acceptance_criteria>
  <done>Full validation is green and local run/test commands are documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` exits 0.
- [ ] `npm run test -- --run` exits 0.
- [ ] `npm run test:e2e` exits 0.
- [ ] Playwright verifies no horizontal scroll at mobile widths.
- [ ] Source/privacy assertions verify no account or child data collection primitives.
</verification>

<success_criteria>
- Automated tests cover all Phase 1 requirements.
- Mobile browser checks prove the shell works at representative phone widths.
- README explains how to run and verify the app locally.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mobile-app-shell/01-03-SUMMARY.md`
</output>
